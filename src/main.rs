use legalize::legalize::LegalBlock;

fn main() {
    println!("Hello, world!");

    let x = legalize::legalize::LegalBlock{
        tag: 0,
        x: 0.0,
        y: 0.0,
        h: 0.0,
        w: 0.0
    };
}
